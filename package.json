{
  "name": "edgefinder-mlb-pro",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "EdgeFinder MLB by CogniVault Labs — visual dashboard for the edgefinder-mlb worker",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
